---
const {frontmatter} = Astro.props;
import { Image } from 'astro:assets';
---
<html>
    <style>/* Register your custom font family and tell the browser where to find it. */
        @font-face {
          font-family: 'Montserrat';
          src: url('/fonts/Montserrat-Regular.ttf') format('truetype');
          font-weight: normal;
          font-style: normal;
          font-display: swap;
        }</style>

    <article class="grid bg-gray-300">
        <h3 class="pt-4 pb-4 text-center font-display text-3xl font-bold" >{frontmatter.title}</h3>
    
        <Image class="object-cover h-96 w-full" src={frontmatter.image} alt="trick image" width="960" height="450"/>
        <slot />
  
        {/* <TwoColumnData class='info' frontmatter={frontmatter}/> */}
        <!-- <TwoColumnData2 class="info" data={infoArr} /> -->
  
        <!-- <MarkdownContent
          class="content"
          content={frontmatter.content}
        ></MarkdownContent> -->
  
        <!-- {transitionsHtml} -->
  
        <!-- <TagList class="tags" prefix={"/wiki-circo/tag"} tags={frontmatter.tags}>
          Tags:{" "}
        </TagList> -->
        <div class="date">{frontmatter.date}</div>
      </article>
</html>